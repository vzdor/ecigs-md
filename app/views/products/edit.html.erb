<% title("Edit - #{h(@product.title)}") %>

<%= form_for @product do |f| %>
  <%= render :partial => "form", :locals => {:f => f} %>
  <%= submit_tag "Update" %> <%= link_to "Cancel", @product %>
<% end %>
