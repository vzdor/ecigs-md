<% title("Edit - #{h(@product.title)}") %>

<%= form_for @product, :html => {:multipart => true} do |f| %>
  <%= render :partial => "form", :locals => {:f => f} %>
  <p><%= submit_tag "Update" %> <%= link_to "Cancel", @product %></p>
<% end %>
